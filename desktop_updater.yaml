# Configuration for the desktop_updater release CLI.
# Hosted on GitHub Pages (static files over HTTPS; supports the nested
# releases/<version>/<platform>/ layout the tool generates). The OTA feed lives
# here; the human-download ZIP can additionally be attached to a GitHub Release.
updates:
  baseUrl: https://easyscripter.github.io/flow_fusion/
